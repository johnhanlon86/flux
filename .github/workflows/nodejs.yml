name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-20.04

    # strategy:
    #   matrix:
    #     node-version: [14.x]

    # services:
    #   mongodb:
    #     image: mongo:4.2
    #     ports:
    #       - 27017:27017

    steps:
    - uses: actions/checkout@v1
    # - name: Use Node.js ${{ matrix.node-version }}
    #   uses: actions/setup-node@v1
    #   with:
    #     node-version: ${{ matrix.node-version }}
    # - name: npm install, build, and test
    #   run: |
    #     npm i
    #     npm run ciconfig
    #     npm run homebuild
    #     npm run test:zelback
    #     npm run test:zelback:unit
    #   env:
    #     CI: true
    # - name: Upload coverage to Codecov
    #   uses: codecov/codecov-action@v2
    # https://github.com/cpina/github-action-push-to-another-repository
    - name: Push services directory to another GitHub repository to build JSDocs separately
      uses: johnhanlon86/flux@actions-push-services-directory-to-other-repo # RunOnFlux/flux@master
      env:
        API_TOKEN_GITHUB: ${{ secrets.API_TOKEN_GITHUB }} # Trunk to create secret (https://github.com/settings/tokens)
      with:
        source-directory: 'ZelBack/src/services/'
        destination-github-username: 'johnhanlon86' # 'RunOnFlux'
        destination-repository-name: 'fluxjsdocs' # '<NAME-OF-NEW-JSDOCS-REPO>' # To be updated by TheTrunk
        target-branch: master
        # target-directory: services/ # This directory needs to be created when the repo is set up. There should be a specific directory for these files to be overwritten on each push. The Actions .yml file will be kept outside this directory so it is not overwritten.
        user-email: johnhanlon86@gmail.com # To be updated to tadeas@zel.network / TheTrunk to confirm if this needs to be a runonflux.io email address
